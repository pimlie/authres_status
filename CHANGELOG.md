# Changelog

All notable changes to `authres_status` should be documented in this file.

## [Unreleased]

- Ongoing development builds use `authres_status::PLUGIN_VERSION` with a `+dev` suffix until the next release is cut.

## [0.7.0] - 2026-04-11

- Formalized the plugin's self-metadata through `authres_status::PLUGIN_VERSION` and `authres_status::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.
